#!/usr/bin/env bash
# Builds the native mazer Rust library (cdylib) for the host machine
# and stages libmazer.{dylib,so} + mazer.h into ./native/.
#
# Implemented in Stage 1.
set -euo pipefail
echo "build.sh: Stage 1 not yet implemented" >&2
exit 1
